# 登录凭据不再通过配置文件提供。
#
# 现在 B 站 Cookie 由 Web 管理页的"扫码登录"获取，并持久化在数据库
# （credentials 表）中，是唯一登录态来源：
#   1. 启动服务:  go run ./cmd/suika -conf ./configs
#   2. 打开前端:  cd web && npm install && npm run dev
#   3. 页面右上角点击"登录"，用哔哩哔哩 App 扫码确认。
#
# conf.Recorder.cookie 字段已废弃且不再被读取；即使这里填了值也会被忽略。
# 无需再复制本文件为 credentials.yaml。
