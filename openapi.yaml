# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: RoomService API
    description: |-
        RoomService exposes the read-only status of the configured rooms:
         one entry per room with its live state and recording progress.
    version: 0.0.1
paths:
    /v1/rooms/list:
        get:
            tags:
                - RoomService
            description: |-
                ListRooms returns the live/record status of every configured room.
                 The room set is fixed by configuration, so no pagination is offered.
            operationId: RoomService_ListRooms
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/room.v1.ListRoomsResponse'
components:
    schemas:
        room.v1.ListRoomsResponse:
            type: object
            properties:
                rooms:
                    type: array
                    items:
                        $ref: '#/components/schemas/room.v1.RoomStatus'
                    description: Status of every configured room, in configuration order.
            description: ListRoomsResponse is the output for RoomService.ListRooms.
        room.v1.RoomStatus:
            type: object
            properties:
                roomId:
                    type: string
                    description: Bilibili room id.
                name:
                    type: string
                    description: Streamer display name used for the recording directory.
                enabled:
                    type: boolean
                    description: Whether this room is monitored at all.
                liveStatus:
                    type: integer
                    description: Broadcast state reported by Bilibili.
                    format: enum
                recordStatus:
                    type: integer
                    description: Recorder state for this room.
                    format: enum
                currentFile:
                    type: string
                    description: Path of the segment file currently being written. Empty when idle.
                bytesWritten:
                    type: string
                    description: Bytes written across the current session. Zero when idle.
                sessionStartedAt:
                    type: string
                    description: Time at which the current session started. Absent when idle.
                    format: date-time
                lastError:
                    type: string
                    description: Last error observed for this room. Empty when healthy.
            description: RoomStatus is the combined live/record status of one configured room.
tags:
    - name: RoomService
